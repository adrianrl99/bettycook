import 'package:flutter/material.dart';

class IngredientsTabWidget extends StatelessWidget {
  const IngredientsTabWidget({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Container(),
    );
  }
}
