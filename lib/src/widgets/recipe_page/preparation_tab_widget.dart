import 'package:flutter/material.dart';

class PreparationTabWidget extends StatelessWidget {
  const PreparationTabWidget({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Container(),
    );
  }
}
