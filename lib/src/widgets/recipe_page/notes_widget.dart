import 'package:flutter/material.dart';

class NotesWidget extends StatelessWidget {
  final int id;
  const NotesWidget({required this.id, Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
